/**
 * ==[ LESSON 0: Setup Variables ]======================================================================================
 */

// these are meant to be proper constants, i.e. we won't change them at all
const MIN_NUMBER = 1
const MAX_NUMBER = 9
const SUITS = ['bamboo', 'characters', 'circles']
const WINDS = ['north', 'south', 'east', 'west']
const DRAGONS = ['red', 'green', 'white']

// we will modify this (which is why i styled it with lowercase)
const allTiles = []
